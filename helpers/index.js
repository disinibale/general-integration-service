const validateFormat = require('./validate')
const formatLastMessage = require('./formatLastMessage')
const formatMessage = require('./formatMessage')
const formatInstanceSettings = require('./formatInstanceSettings')

module.exports = {
    validateFormat,
    formatLastMessage,
    formatMessage,
    formatInstanceSettings
}